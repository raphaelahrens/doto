"""
In cli.cmd are all the CLI commands that can be called with
    doto <command>

The only purpose of this mdoule is to hold the commands in one place so it is easy
for the main doto script to load the commands dynamically.
"""
